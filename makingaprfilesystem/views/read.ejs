<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    
    <div class="w-full h-full bg-zinc-900 p-10 text-white">
        <a class="bg-blue-500 mb-5 px-3 py-2 rounded-lg" href="/">Back to Home</a>
        <h1 class="text-3xl tracking-tighter mb-3 mt-7">All User</h1>
        <div class="users flex gap-3 flex-wrap ">
           
                <% users.forEach(function(user){ %>
                    <div class="user max-w-72 p-4 bg-zinc-800 rounded-lg">
                        <div class="w-full h-72 bg-zinc-700 overflow-hidden">
                            <img class="w-full h-full object-cover object-top" src="<%= user.imageurl %>" alt="Image Not Loaded">
                        </div>
                        <h4 class="text-2xl tracking-tighter mt-3"><%= user.name %></h4>
                        <h5 class="text-zinc-500"><%= user.email %></h5>
                        <div class="user gap-3 mt-5">
                            <a class="text-green-400" href="edit/<%= user._id%>">Edit User</a>
                            <a class="text-red-500 px-8" href="delete/<%= user._id%>" >Delete User</a>
                        </div>
                    </div>
               <% })  %>
     
            
        </div>

    </div>
    
</body>
</html>