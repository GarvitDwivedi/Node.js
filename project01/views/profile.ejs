<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end">
            <a class="bg-red-600 px-3 py-2 rounded-md ml-5 inline-block " href="/login">Logout</a>
        </div>
        <h1 class="text-3xl mb-5">Hello <%= user.name %></h1>
        <h3>You can create a post</h3>
        <form  method="post" action="/post">
            <textarea class="block p-3 resize-none bg-transparent outline-none border-2 border-zinc-700 w-1/3 mt-2 rounded-md" placeholder="What's in your Mind??" name="content" ></textarea>
            <input class="block px-3 py-2 bg-blue-600 mt-2 w-40 rounded-lg" type="submit" value="Create Post">
        </form>

        <h1 class="text-3xl mt-10 text-zinc-400">Posts</h1>
       <% user.posts.reverse().forEach(function(post){ %>
        <div class="postContinaer mt-5">
            <div class="post w-1/3 p-4 border-[1px] rounded-md bg-zinc-800 border-zinc-700 mb-2">
                <h4 class="text-blue-600 mb-2">@<%= user.username %></h4>
                <p class="text-sm tracking-tight"> <%= post.content %> </p>
                <small class="mt-2 inline-block"><%= post.likes.length%>Likes</small>
                <div class="btns flex mt-1 gap-10">
                    <a class="text-blue-600 " href="/like/<%= post._id %>"> 
                        <%= post.likes.indexOf(user._id) === -1 ? "like" : "unlike" %>
                    </a>
                    <a class="text-yellow-500" href="/edit/<%= post._id %>">Edit</a>
                </div>
            </div>
        </div>
     <%  })%>
        
    </div>
</body>
</html>